import './index.css';
import { VideoPixelate } from './src';

(async () => {
	// const { initWebglApp } = await import('./src');
	// const canvas = document.getElementById('canvas') as HTMLCanvasElement;
	// initWebglApp({ canvas, debug: true, controls: false });

	const videopixelate = document.querySelector(VideoPixelate.name) as VideoPixelate;

	// if (!videopixelate) return;
	// setInterval(() => {
	// 	videopixelate.next();
	// }, 3000);
})();
